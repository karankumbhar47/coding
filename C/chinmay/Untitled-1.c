#include <stdio.h>
int main() {
    float amount;
    int a,k
   printf("Enter the amount withdrawn in hundred \n");
   scanf("%d", amount);
   k=amount*100
   a=k/100
  printf("Notes of denomination of  \n"); 
   return 0;
}
